import 'package:flutter/material.dart';

class CustomSpaceHeight extends StatelessWidget {
  const CustomSpaceHeight({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 15,
    );
  }
}