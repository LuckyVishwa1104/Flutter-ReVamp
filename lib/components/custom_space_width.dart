import 'package:flutter/material.dart';

class CustomSpaceWidth extends StatelessWidget {
  const CustomSpaceWidth({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 15,
    );
  }
}
